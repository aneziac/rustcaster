fn main() {
    println!("{}", raycast(dir, true));
    // println!("Hello, world!");
}

struct Player {
    x: f64,
    y: f64,
    dir: f64
}

fn raycast(angle: f64, x: bool) {
    player = Player {x: 80.0, y: 80.0, dir: 0.01};
    if x {
        player_discrete: float64, player_cont: float64 = player.x, player.y;
    }
    player_discrete ;
}
