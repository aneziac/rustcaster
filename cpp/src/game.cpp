#include "../include/game.hpp"


Game::Game(Player *player, World *world, bool debug = false, bool projectionType = false) :
           player(player), world(world), projectionType(projectionType) {
    // projPlaneDist =
}

double Game::raycast(double angle, bool x) {

}
